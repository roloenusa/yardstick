<%= form_for(@testsuite, html: { class: "form-horizontal" }) do |f| %>
  <% if @testsuite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testsuite.errors.count, "error") %> prohibited this testsuite from being saved:</h2>

      <ul>
      <% @testsuite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title, class: 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_field :title, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :description, class: 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_area :description, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit 'Submit', class: 'btn btn-default' %>
    </div>
  </div>
<% end %>

<%= render 'testcases' unless @testsuite.new_record? %>
