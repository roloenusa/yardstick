$("#modal-window").html("<%= escape_javascript(render 'test_results/run') %>");
// Hide modal on submit.
$('#modal-window').click(function(e) {
    $('form').submit(function() {
      $('#modal-window').modal('hide');
    });
});
